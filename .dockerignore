# =============================================================================
# .dockerignore — excludes files from the Docker build context
# Keeps the build fast and the image lean.
# =============================================================================

# Git internals — never needed inside the image
.git/
.gitignore

# Python cache / compiled files
__pycache__/
*.py[cod]
*.so
*.egg-info/
.eggs/
dist/
build/

# Jupyter notebook checkpoints
.ipynb_checkpoints/
Dataviz.ipynb

# Local test outputs and snakemake state — not needed in image
.snakemake/
test_out/
test_data/test_out/
short_read_qc.out
short-read-quality-control.out

# test_data configs are auto-generated by setup.sh locally; we'll
# generate container-appropriate ones in Step 6 instead
test_data/samples.csv
test_data/parameters.yaml

# OS / IDE noise
*.DS_Store
.idea/
*.log

# Docs — not needed at runtime
README.md
AUTHORS.rst
LICENSE
*.rst

# setup.sh is for local installs only — not used in Docker build
setup.sh
